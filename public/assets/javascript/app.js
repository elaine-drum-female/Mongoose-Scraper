$(document).ready(function(){
 $("#article-contents").empty();
 $("#message").text("No new articles at this time!");

 // When a user clicks on the New scrape button, 
 // Needed to use a get route from the cnn website to scrape ...BUT that's already been complete in server.js 
 //I wanted to have the div #article-contents show a modal or something say "Scrape complete" instead of having a get route for /scrape...it's in the server.js 
 // Next I wanted to retrieve from my database the collected articles
 
 

});
  

 